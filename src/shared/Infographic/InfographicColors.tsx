type Budgets = {
    category: string,
    maximum: number,
    theme: string
}[]

type Transactions= {
    avatar: string,
    name: string,
    category: string,
    date: string,
    amount: number,
    recurring: boolean,
}[]


export default function InfographicColors ( totalBudgetSpending: number ) {

    const budgetsAmounted = ""

    return ""
}